const postLogin = require('./postLogin')
const postRegister = require('./postRegister')
const postMessage = require('./contact')

exports.controllers = {
    postLogin,
    postRegister,
    postMessage
}